# 需要用户确认的歧义问题（已确认更新）

已确认：
1. 默认电离后端以 2022 文献为准（默认 Popruzhenko），但保留 Talebpour 与 Popruzhenko 双接口。
2. 初始横向包络：Gaussian + lens phase。
3. 时间包络：Gaussian，脉宽定义为 intensity FWHM。
4. f/240：按 1/e^2 口径定义。
5. z=0 定义：入射面，后处理平移到 vacuum focus。
6. 初始啁啾：0。
7. 自陡峭相关逆算符：严格频域逆算符。
8. 维度：先做 2D+1 轴对称原型。

仍需后续阶段确认：
- 与实验对比时输入光束口径截断细节（是否显式光阑）。
- 完整 Eq.(27) 算符离散细节在升级到全3D+1时的数值格式。
